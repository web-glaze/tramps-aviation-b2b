/**
 * @deprecated  Removed.
 *
 * The B2B portal switched from a left sidebar to a top navbar; this file was
 * already unused (the only references were in DevPathBar's metadata list).
 * Safe to delete:
 *
 *   rm components/layout/B2BSidebar.tsx
 *
 * Use `Header` from `./Header.tsx` for the portal's top navigation instead.
 */
export {};
